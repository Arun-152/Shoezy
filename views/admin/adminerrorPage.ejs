<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin - 500 Error</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Poppins', sans-serif;
            min-height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            background: linear-gradient(120deg, #1e3c72, #2a5298, #6b7280);
            overflow: hidden;
        }

        .error-container {
            text-align: center;
            padding: 40px;
            background: rgba(255, 255, 255, 0.15);
            backdrop-filter: blur(12px);
            border-radius: 20px;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.3);
            animation: slideIn 0.8s ease-out;
            max-width: 500px;
        }

        h1 {
            font-size: 130px;
            color: #60a5fa;
            text-shadow: 0 0 15px rgba(96, 165, 250, 0.7), 3px 3px 5px rgba(0, 0, 0, 0.2);
            letter-spacing: 8px;
            animation: pulse 2s infinite;
        }

        p {
            font-size: 24px;
            color: #e5e7eb;
            font-weight: 300;
            margin-top: 10px;
            text-shadow: 1px 1px 3px rgba(0, 0, 0, 0.2);
        }

        .admin-note {
            font-size: 16px;
            color: #93c5fd;
            margin-top: 15px;
            font-style: italic;
        }

        .home-btn {
            display: inline-block;
            margin-top: 20px;
            padding: 12px 24px;
            background: rgba(96, 165, 250, 0.8);
            color: #fff;
            text-decoration: none;
            font-weight: 500;
            border-radius: 8px;
            box-shadow: 0 4px 15px rgba(0, 0, 0, 0.2);
            transition: background 0.3s, transform 0.2s;
        }

        .home-btn:hover {
            background: rgba(96, 165, 250, 1);
            transform: translateY(-2px);
        }

        .background-effect {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            z-index: -1;
        }

        .particle {
            position: absolute;
            background: rgba(255, 255, 255, 0.25);
            border-radius: 50%;
            animation: drift 25s infinite linear;
        }

        .particle:nth-child(1) {
            width: 70px;
            height: 70px;
            top: 15%;
            left: 20%;
            animation-duration: 18s;
        }

        .particle:nth-child(2) {
            width: 90px;
            height: 90px;
            top: 55%;
            left: 75%;
            animation-duration: 20s;
        }

        .particle:nth-child(3) {
            width: 60px;
            height: 60px;
            top: 65%;
            left: 35%;
            animation-duration: 23s;
        }

        @keyframes slideIn {
            from {
                transform: translateY(50px);
                opacity: 0;
            }
            to {
                transform: translateY(0);
                opacity: 1;
            }
        }

        @keyframes pulse {
            0%, 100% {
                transform: scale(1);
                text-shadow: 0 0 15px rgba(96, 165, 250, 0.7);
            }
            50% {
                transform: scale(1.05);
                text-shadow: 0 0 25px rgba(96, 165, 250, 1);
            }
        }

        @keyframes drift {
            0% {
                transform: translate(0, 0);
            }
            50% {
                transform: translate(25px, -25px);
            }
            100% {
                transform: translate(0, 0);
            }
        }

        @media (max-width: 600px) {
            h1 {
                font-size: 80px;
                letter-spacing: 4px;
            }
            p {
                font-size: 18px;
            }
            .admin-note {
                font-size: 14px;
            }
            .error-container {
                padding: 20px;
            }
            .home-btn {
                padding: 10px 20px;
                font-size: 14px;
            }
        }
    </style>
</head>
<body>
    <div class="background-effect">
        <div class="particle"></div>
        <div class="particle"></div>
        <div class="particle"></div>
    </div>
    <div class="error-container">
        <h1>500</h1>
        <p>Error: Internal Server Error</p>
        <p class="admin-note">Admin: Check server logs or configuration for issues.</p>
        <a href="/admin/dashboard" class="home-btn">Back to Dashboard</a>
    </div>
</body>
</html>